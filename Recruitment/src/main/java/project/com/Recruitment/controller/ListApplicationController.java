package project.com.Recruitment.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
// import project.com.Recruitment.service.ListApplicationService;

@RestController
// @RequestMapping("/person")
public class ListApplicationController {

    // @Autowired
    // private ListApplicationService listApplicationService;

    // login via login.html filen 
    // @PostMapping("/login")
    // public ResponseEntity<String> login(@RequestParam String username, @RequestParam String password) {
    //     boolean validUser = rersonService.validateUser(username, password); //skicka till service för databas hantering
    //     if (validUser) {
    //         return ResponseEntity.ok("Login successful for " + username);
    //     } else {
    //         return ResponseEntity.status(401).body("Invalid credentials");
    //     }
    // }
}
